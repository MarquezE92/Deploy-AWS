const  mongoose = require('mongoose'); 

const infoEsquema = new mongoose.Schema(
{
	content: {
        type: String,   
        required: true, 
    }    
}
);

module.exports = mongoose.model('Info', infoEsquema)